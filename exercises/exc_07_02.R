nominal <- c("High", "Low", "High", "Low", "Medium")

rank <- factor(nominal, order = TRUE, 
       levels = c("Low", "Medium", "High"))

nominal1 <- nominal[1]

nominal2 <- nominal[2]

rank1 <- rank[1]

rank2 <- rank[2]

___ > ___
